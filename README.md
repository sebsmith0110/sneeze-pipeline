# Sneeze Pipeline 

An AWS ECS pipeline that:
- Reads sneezing data from Gmail via IMAP
- Enriches with weather and air-quality data
- Stores daily updates in an S3 bucket (stored in M&C Saatchi Fluency's AWS account)